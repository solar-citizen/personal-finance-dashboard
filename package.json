{
  "name": "personal-finance-dashboard",
  "version": "0.0.1",
  "author": "solar_citizen",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "bun@1.3.1",
  "license": "GPL-3.0-only",
  "scripts": {
    "dev:web": "cd apps/web && bun dev",
    "dev:api": "cd apps/api && bun start:dev",
    "dev": "bun run dev:web & bun run dev:api",
    "build:web": "cd apps/web && bun run build",
    "build:api": "cd apps/api && bun run build",
    "build": "bun run build:web && bun run build:api",
    "bump": "bunx npm-check-updates -u --workspaces --root && bun install && bun run update:bun",
    "bump:interactive": "bunx npm-check-updates -i --workspaces --root && bun install && bun run update:bun",
    "bump:minor": "bunx npm-check-updates -u --workspaces --root --target minor && bun install && bun run update:bun",
    "bump:patch": "bunx npm-check-updates -u --workspaces --root --target patch && bun install && bun run update:bun",
    "update:bun": "bun run bin/update-bun.js"
  },
  "devDependencies": {
    "@types/lodash-es": "^4.17.12",
    "dotenv": "^17.2.3",
    "turbo": "^2.6.0"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "lodash-es": "^4.17.21",
    "zod": "^4.1.12"
  }
}
